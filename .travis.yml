dist: trusty 
language: node_js
node_js:
  - "9"
before_script:
  - npm install -g gatsby-cli
script:
  - gatsby build
after_success:
  - npm run deploy

branches:
  only:
    - master

cache:
  directories:
    -  node_modules
